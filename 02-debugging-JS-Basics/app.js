console.log(`helloo world`);

const myVariable = "yada yada yada";

let init = 0;

debugger; //Sets a manual breakpoint in the code - the code stops and the next lines can be checked through.
while (init < 50) {
  init++;
  console.log(`this is iteration number ${init}`);
}
